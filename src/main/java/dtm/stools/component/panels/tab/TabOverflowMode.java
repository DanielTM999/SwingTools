package dtm.stools.component.panels.tab;

/** Defines how a single tab row exposes tabs that do not fit in the available space. */
public enum TabOverflowMode {

    /** Shows one compact overflow button that opens the hidden tabs in a popup. */
    MENU,

    /** Shows previous and next chevron buttons. */
    SCROLL_BUTTONS
}
